---
// import { getCollection } from "astro:content";
import PageLayout from "@/layouts/Base";
// import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { MY_SKILL, PLAN_TO_LEARN, PROJECTS } from "@/data/constants";
// import { sortMDByDate } from "@/utils";
import { t, changeLanguage } from "i18next";

changeLanguage("cs");
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title mb-6">
			Hello World! <span class="text-red-600">UNDER CONSTRUCTION 🚧</span>
		</h1>
		<p class="mb-4 text-base">
			Hi, I’m a theme for Astro, an example of how you can use this starter to create a
			personal blog. If you want to know more about how you can customise me, add more posts,
			and make it your own, click on one the github icon link below which will take you to my
			repo.
		</p>
		<SocialList />
	</section>
	<section class="mt-6">
		<h2 class="title mb-4 text-xl">{t("technologie")}</h2>
		{
			MY_SKILL.map((skill) => (
				<div class="mr-2 mb-2 inline-block bg-textColor px-4 py-2 text-base font-semibold text-bgColor">
					{skill.title}
				</div>
			))
		}
		<!-- <h2 class="text-l mb-4 font-bold">{t("future-technologie")}</h2>
		{
			PLAN_TO_LEARN.map((skill) => (
				<div class="text-md mr-2 mb-2 inline-block bg-textColor px-4 py-2 font-semibold text-bgColor">
					{skill.title}
				</div>
			))
		} -->
	</section>
	<!-- <section aria-label="Blog post list" class="mt-6">
		<h2 class="title mb-4 text-xl">{t("projects")}</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col flex-wrap gap-x-2 sm:flex-row [&_q]:w-full">
						<PostPreview post={p} withDesc={true} />
					</li>
				))
			}
		</ul>
	</section> -->
	<section class="mt-6">
		<h2 class="title mb-4 text-xl">{t("projects")}</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				PROJECTS.map(({ href, title, desc }) => (
					<li>
						<a
							href={href}
							target="_blank"
							rel="noopener noreferrer"
							class="cactus-link inline-block"
						>
							{title}
						</a>
						:<p class="inline-block sm:mt-2">{desc}</p>
					</li>
				))
			}
		</ul>
	</section>
</PageLayout>
